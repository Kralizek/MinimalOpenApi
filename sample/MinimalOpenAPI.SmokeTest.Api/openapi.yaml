openapi: "3.0.0"
info:
  title: Smoke Test API
  version: "1.0.0"
paths:
  /ping:
    get:
      operationId: ping
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
